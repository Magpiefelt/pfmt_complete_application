# Environment defaults for PFMT application

# ---- App ----
NODE_ENV=development

# Secret used by the backend to sign JWTs.  Change this in production.
JWT_SECRET=change-me

# Frontend API base URL.  In development the frontend runs on port 5173 and proxies to the backend on port 3000.
VITE_API_BASE_URL=http://localhost:3000/api

# Backend HTTP port and CORS origin.  Adjust PORT if you map to a different host port.
PORT=3000
CORS_ORIGIN=http://localhost:5173

# ---- Database ----
# Postgres credentials.  These defaults are used by the docker-compose files.
POSTGRES_USER=pfmt
POSTGRES_PASSWORD=pfmt
POSTGRES_DB=pfmt

# Database host and port the backend should connect to.  In docker-compose the service name is "db".
DB_HOST=db
DB_PORT=5432

# ---- PGAdmin (optional) ----
# Credentials for the pgAdmin container exposed only in development compose.  You can change or remove these if you don't use pgAdmin.
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin